public class Area {
    public static void main(String[] args) {
        int radius = 7;
        double area = Math.PI * radius * radius;   // Using Java’s built-in PI
        System.out.printf("Area of circle is: %.2f", area);
    }
}
